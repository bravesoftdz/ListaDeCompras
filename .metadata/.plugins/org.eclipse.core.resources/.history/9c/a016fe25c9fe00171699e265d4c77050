package controllers;

import java.awt.EventQueue;
import java.awt.Image;

import javax.swing.JFrame;
import javax.swing.JButton;
import javax.swing.JLabel;
import javax.swing.ImageIcon;

public class ListaComprasController {

	private JFrame frame;

	/**
	 * Launch the application.
	 */
	public static void main(String[] args) {
		EventQueue.invokeLater(new Runnable() {
			public void run() {
				try {
					ListaComprasController window = new ListaComprasController();
					window.frame.setVisible(true);
				} catch (Exception e) {
					e.printStackTrace();
				}
			}
		});
	}

	/**
	 * Create the application.
	 */
	public ListaComprasController() {
		initialize();
	}

	/**
	 * Initialize the contents of the frame.
	 */
	private void initialize() {
		frame = new JFrame();
		frame.setBounds(100, 100, 450, 300);
		frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
		frame.getContentPane().setLayout(null);
		
		JLabel lblNewLabel = new JLabel("");
		
		ImageIcon ima = new ImageIcon(ListaComprasController.class.getResource("/imgs/family.jpg"));
		Image test = ima.getImage();
		Image text2 = test.getScaledInstance(lblIesp.getWidth(), lblIesp.getHeight(), Image.SCALE_DEFAULT);
		Icon ico = new ImageIcon(text2);
		//lblNewLabel.setIcon
		lblNewLabel.setBounds(10, 11, 414, 239);
		frame.getContentPane().add(lblNewLabel);
	}
}
